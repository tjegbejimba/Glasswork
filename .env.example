# Glasswork Environment Variables
# Copy to your shell profile or set as User environment variables.

# ADO and GitHub integration is handled by agents (Copilot CLI, etc.) who
# read/write task files directly in the vault and file issues via the
# triage-issue skill. No PAT needed in the app itself.
