<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Glasswork.Pages.BacklogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:conv="using:Glasswork.Converters"
    xmlns:controls="using:Glasswork.Controls"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <Page.Resources>
        <conv:BoolVisibilityConverter x:Key="BoolVis" />
        <conv:SubtaskSegmentBrushConverter x:Key="SegBrush" />
        <conv:DueUrgencyBrushConverter x:Key="DueBg" />
        <conv:DueUrgencyForegroundBrushConverter x:Key="DueFg" />

        <DataTemplate x:Key="BacklogTaskTemplate">
            <Grid Padding="10,8" RowDefinitions="Auto,Auto"
                  DoubleTapped="TaskRow_DoubleTapped"
                  Background="Transparent">
                <!-- Title row -->
                <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto,Auto,Auto,Auto">
                    <Grid Grid.Column="0" Width="22" Height="22" VerticalAlignment="Center" Margin="0,0,8,0">
                        <CheckBox VerticalAlignment="Center" HorizontalAlignment="Center"
                                  IsChecked="{Binding IsDone, Mode=OneWay}"
                                  Style="{StaticResource CircleCheckBoxStyle}"
                                  IsHitTestVisible="False" IsTabStop="False" />
                        <Button Background="Transparent" BorderThickness="0" Padding="0"
                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                Click="TaskCheckbox_Click" />
                    </Grid>
                    <HyperlinkButton Grid.Column="1" Content="{Binding Title}"
                                     Click="OpenTask_Click"
                                     Padding="0" VerticalAlignment="Center"
                                     HorizontalAlignment="Stretch"
                                     HorizontalContentAlignment="Left" />
                    <Border Grid.Column="2" Style="{StaticResource ChipBorderStyle}"
                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                            Visibility="{Binding HasPriorityChip, Converter={StaticResource BoolVis}}">
                        <TextBlock Text="{Binding Priority}" Style="{StaticResource ChipTextStyle}" />
                    </Border>
                    <Border Grid.Column="3" Style="{StaticResource ChipBorderStyle}"
                            Background="{Binding DueUrgency, Converter={StaticResource DueBg}}"
                            Visibility="{Binding HasDue, Converter={StaticResource BoolVis}}">
                        <TextBlock Text="{Binding DueChipText}" Style="{StaticResource ChipTextStyle}"
                                   Foreground="{Binding DueUrgency, Converter={StaticResource DueFg}}" />
                    </Border>
                    <Button Grid.Column="4" Click="ToggleMyDay_Click"
                            ToolTipService.ToolTip="Toggle My Day"
                            Background="Transparent" BorderThickness="0"
                            Padding="6,2" VerticalAlignment="Center" Margin="6,0,0,0">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE706;" FontSize="13" />
                    </Button>
                    <Button Grid.Column="5" Background="Transparent" BorderThickness="0"
                            Padding="6,2" VerticalAlignment="Center">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE712;" FontSize="13" />
                        <Button.Flyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Mark To Do" Click="SetTodo_Click" />
                                <MenuFlyoutItem Text="Mark In Progress" Click="SetInProgress_Click" />
                                <MenuFlyoutItem Text="Mark Done" Click="SetDone_Click" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem Text="Open in Obsidian" Click="OpenTaskInObsidian_Click" />
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>
                </Grid>

                <!-- Card details -->
                <StackPanel Grid.Row="1" Spacing="6" Margin="32,6,0,2"
                            Visibility="{Binding ShowCardDetails, Converter={StaticResource BoolVis}}">
                    <ItemsControl ItemsSource="{Binding Subtasks}"
                                  Visibility="{Binding UseSegmentedBar, Converter={StaticResource BoolVis}}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Spacing="3" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="22" Height="6" CornerRadius="2"
                                        Background="{Binding Converter={StaticResource SegBrush}}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8"
                          Visibility="{Binding UseContinuousBar, Converter={StaticResource BoolVis}}">
                        <ProgressBar Grid.Column="0" Minimum="0" Maximum="1"
                                     Value="{Binding ProgressFraction}" />
                        <TextBlock Grid.Column="1" Text="{Binding ProgressLabel}"
                                   FontSize="11" Opacity="0.65" VerticalAlignment="Center" />
                    </Grid>
                    <StackPanel Orientation="Horizontal" Spacing="6"
                                Visibility="{Binding HasCurrentStep, Converter={StaticResource BoolVis}}">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE768;" FontSize="11" Opacity="0.6" />
                        <TextBlock Text="{Binding CurrentStepText}" FontSize="12" Opacity="0.8"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="6"
                                Visibility="{Binding HasBlocker, Converter={StaticResource BoolVis}}">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE7BA;" FontSize="11"
                                  Foreground="#C50F1F" />
                        <TextBlock Text="{Binding FirstBlockerText}" FontSize="12"
                                   Foreground="#C50F1F" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                    <controls:VaultMarkdownView Markdown="{Binding Description}"
                                               AutomationProperties.Name="{Binding BlurbPreview}"
                                               Opacity="0.8"
                                               Loaded="OnBlurbMarkdownLoaded"
                                               Unloaded="OnBlurbMarkdownUnloaded"
                                               Visibility="{Binding HasBlurb, Converter={StaticResource BoolVis}}" />
                </StackPanel>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="BacklogGroupHeaderTemplate">
            <Grid Padding="6,10,10,4" Background="Transparent"
                  Tapped="GroupHeader_Tapped"
                  ContextRequested="GroupHeader_ContextRequested"
                  ColumnDefinitions="Auto,*,Auto">
                <FontIcon x:Name="GroupHeaderChevron" Grid.Column="0" FontFamily="Segoe Fluent Icons"
                          Glyph="{Binding IsCollapsed, Converter={StaticResource ChevronGlyph}}"
                          FontSize="11" Margin="0,0,8,0" Opacity="0.7" VerticalAlignment="Center" />
                <TextBlock x:Name="GroupHeaderText" Grid.Column="1" Text="{Binding DisplayHeader}"
                           Style="{StaticResource SubtitleTextBlockStyle}" FontSize="14"
                           FontWeight="SemiBold" Opacity="0.85" VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           ToolTipService.ToolTip="{Binding AdoUrl}" />
                <TextBlock Grid.Column="2" Text="{Binding TotalCount}"
                           FontSize="12" Opacity="0.55" VerticalAlignment="Center"
                           Margin="8,0,0,0" />
            </Grid>
        </DataTemplate>

        <controls:BacklogRowTemplateSelector x:Key="BacklogRowSelector"
            TaskTemplate="{StaticResource BacklogTaskTemplate}"
            GroupHeaderTemplate="{StaticResource BacklogGroupHeaderTemplate}" />

        <conv:CollapseChevronGlyphConverter x:Key="ChevronGlyph" />
    </Page.Resources>

    <Grid Padding="32" RowDefinitions="Auto,Auto,*">

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Backlog" />

        <!-- Filter bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,16,0,16">
            <ComboBox x:Name="StatusFilter" SelectedIndex="0" SelectionChanged="StatusFilter_Changed" Width="160">
                <ComboBoxItem Tag="all" Content="Active (not done)" />
                <ComboBoxItem Tag="todo" Content="To Do" />
                <ComboBoxItem Tag="in-progress" Content="In Progress" />
                <ComboBoxItem Tag="done" Content="Done" />
            </ComboBox>

            <ToggleButton x:Name="GroupToggle"
                          IsChecked="{x:Bind ViewModel.IsGrouped, Mode=TwoWay}"
                          ToolTipService.ToolTip="Group by parent">
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE71D;" FontSize="13" />
            </ToggleButton>

            <Button Content="&#xE710;" FontFamily="Segoe Fluent Icons" Click="AddTask_Click" ToolTipService.ToolTip="New task" />
        </StackPanel>

        <!-- Task list (mixed: group headers + tasks when grouped, tasks only otherwise) -->
        <ListView
            Grid.Row="2"
            x:Name="TaskList"
            ItemsSource="{x:Bind ViewModel.Rows}"
            ItemTemplateSelector="{StaticResource BacklogRowSelector}"
            SelectionMode="None"
            IsItemClickEnabled="False">
            <ListView.KeyboardAccelerators>
                <KeyboardAccelerator Key="O" Modifiers="Control,Shift" Invoked="OpenInObsidian_Accelerator" />
            </ListView.KeyboardAccelerators>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,2" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- Empty state: shown when the filtered Backlog has no tasks -->
        <controls:EmptyState
            x:Name="EmptyStateView"
            Grid.Row="2"
            Glyph="&#xE8FD;"
            Headline="Nothing in the Backlog yet."
            Body="Capture a task to get started. You can pull anything you add here into My Day later."
            CtaText="New task"
            CtaClicked="EmptyState_NewTask"
            Visibility="Collapsed" />
    </Grid>
</Page>
