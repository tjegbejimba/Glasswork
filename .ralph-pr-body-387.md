## Summary

- adds typed Automation Review Queue proposal payloads, coherent-selection analysis, apply-failure metadata, refresh workflows, and stateful fingerprint staleness checks
- applies meeting-note, status-change, block, unblock, blocker-reason, due-date, subtask, and structured-link proposals through one Core approval seam with one task save
- makes review retries idempotent after a post-save queue-commit interruption and hardens meeting-note rendering so untrusted recap content stays inside task Notes

## Decisions

- the managed Notes subsection uses `### Meeting updates` inside task-level Notes so it stays within the existing `## Notes` parser boundary
- related meeting-note preselection is surfaced as approval analysis suggestions; approval still applies only the explicitly selected item ids
- apply failure metadata is stored on active queue items as the last failure code/message/timestamp so retries remain pending and inspectable without moving to History

## Validation

- `dotnet test tests/Glasswork.Tests/Glasswork.Tests.csproj --configuration Release --nologo --verbosity minimal -- MSTest.Parallelize.Workers=1`
- `dotnet build src/Glasswork.App/Glasswork.csproj --configuration Release -p:Platform=x64 --nologo --verbosity minimal`

## Review notes

- **Resolved:** sanitized meeting-note headings/content so recap text cannot escape the `## Notes` parser boundary or synthesize Related links
- **Resolved:** added approval handlers and regression coverage for `StatusChange`, `UnblockTask`, and `BlockerReasonChange`
- **Resolved:** reject malformed `TaskId` values at submission/approval with structured failures instead of throwing from `VaultService`
- **Resolved:** retry after a simulated post-task-save queue-commit interruption now recognizes already-applied proposals and marks them approved idempotently

Closes #387
