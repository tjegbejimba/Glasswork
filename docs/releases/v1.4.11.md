# Glasswork v1.4.11

Update checks now remain reliable when GitHub's shared anonymous API quota is
exhausted.

## Changes

- Added a complete public Git-tag fallback with expected Release-asset
  verification for both app and MCP update streams.
- Preserved the locally installed MCP version when remote release discovery
  fails, avoiding the incorrect "Not installed" status.

## Validation

- Release workflow gates run.
