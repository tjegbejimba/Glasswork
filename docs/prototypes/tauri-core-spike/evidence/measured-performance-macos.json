{
  "scorecard": "docs/research/cross-platform-prototype-scorecard.md (locked by #376)",
  "platform": "macOS (Apple Silicon, arm64). Windows measurements NOT taken -- no Windows device available to this session.",
  "build_configuration": "Release, built via `npx tauri build` (NOT raw `cargo build --release`).",
  "metric_1_cold_launch_ms": {
    "runs": [
      89,
      86,
      90
    ],
    "median": 89,
    "status": "PROCEDURE_NOT_MET",
    "gap": "The scorecard's Measurement Environment Freeze defines cold launch as preceded by a reboot or equivalent guaranteed cold-cache state. No reboot/`purge` was possible in this environment, so these are warm-cache launches. Value recorded for transparency; it must be re-measured in a frozen environment before it is entered into the calculator."
  },
  "metric_2_interaction_latency_ms": {
    "runs": [
      1,
      1.0000000000002274,
      2
    ],
    "median": 1.0000000000002274,
    "status": "MEASURED",
    "method": "In-page performance.now() delta from the Task Detail navigation click to the Page being rendered, instrumented in src/main.js and read via the WebDriverIO embedded provider. Chosen over OS-level automation because macOS Accessibility permission for this ad-hoc-signed dev binary requires a fresh manual grant on every rebuild."
  },
  "metric_3_idle_memory_mb": {
    "runs": [
      100.828,
      88.922,
      96.391
    ],
    "median": 96.391,
    "status": "MEASURED",
    "method": "`ps -o rss=` against the real OS process, sampled 30s after launch with no interaction -- #370's exact procedure."
  },
  "metric_4_installed_package_size": {
    "macos_app_bundle_kb": 14992,
    "macos_app_bundle_human": "15 MB",
    "macos_dmg_kb": 4388,
    "macos_dmg_human": "4.3 MB",
    "status": "PARTIAL_ONE_OS_ONLY",
    "gap": "The scorecard averages the two OSes and requires both noted in raw evidence. Only macOS is measured; the Windows .msi/.exe size is pending.",
    "caveat": "Includes the test-only WebDriver bridge (tauri-plugin-wdio + tauri-plugin-wdio-webdriver, see src-tauri/Cargo.toml). A real production build would feature-gate these out, so this over-states size relative to a shipping build."
  },
  "metric_5_filewatch_response_latency_ms": {
    "status": "NOT_MEASURED",
    "single_observation_ms": null,
    "gap": "Scorecard requires a 3-run median timestamped from the acceptance recording. Only a single WebDriver-polled wall-clock observation exists (see evidence/filewatch-live-update.json), which proves the pipeline is live but is NOT the metric value. Per the scorecard's Missing-evidence rule this scores 0 unless captured properly before scoring."
  },
  "metric_6_accessibility_completeness_pct": {
    "status": "NOT_MEASURED",
    "gap": "Requires a live VoiceOver (macOS) and Narrator (Windows) pass scoring correct accessible name + role + state per hit zone, averaged across both OSes. Automating a screen reader is not appropriate; this is a HITL step for TJ, and the Windows half additionally needs a Windows device. See README."
  },
  "metric_7_testability_breadth": {
    "value": 6,
    "of": 6,
    "status": "MEASURED",
    "catalog_coverage": {
      "toggle a subtask": "vertical-slice.spec.js",
      "open Task Detail": "vertical-slice.spec.js (+ a dedicated Page-navigation test)",
      "drag-reorder a subtask": "subtask-reorder.spec.js (via the accessible Alt+Arrow keyboard alternative)",
      "trigger the HTML preview": "sandbox-verify.spec.js",
      "trigger a live file-watch update": "filewatch-live-update.spec.js (on the locked 'Confirm Tailscale ACL update' fixture task)",
      "navigate via keyboard": "vertical-slice.spec.js (Planner nav focus-reachability)"
    }
  },
  "metric_8_implementation_complexity_loc": {
    "framework_ui_total": 868,
    "breakdown": {
      "frontend_src_js_html_css_excl_vendor": 644,
      "tauri_ipc_shell_rs_excl_gen": 224
    },
    "bounded_rust_core_loc_reported_separately": 635,
    "status": "MEASURED_WITH_INTERPRETATION_NOTE",
    "note": "The scorecard excludes 'shared Glasswork.Core logic', written with Avalonia/Uno in mind since they reuse the existing C# Core. Tauri has no such reuse path -- its Core is new code this framework required. Both figures are reported so #381 can pick a fair comparison basis rather than one being silently chosen here."
  },
  "overall_status": "INCOMPLETE -- 3 of 8 metrics fully meet their locked procedure (2, 3, 7); metric 8 is measured with an interpretation note; metric 1 does not meet the cold-cache procedure; metric 4 covers one OS; metrics 5 and 6 are not measured. No value here should be entered into the scorecard calculator until the gaps above are closed."
}
